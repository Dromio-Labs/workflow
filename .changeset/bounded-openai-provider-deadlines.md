---
"@dromio/workflow": patch
---

Bound OpenAI-compatible provider requests across connection and response-body
streaming, compose caller cancellation, and emit terminal model failures when
the provider deadline expires.
